# WEB PROJECT EXPORT — {{STYLE_NAME}}

`03_UPLOAD_TO_WEB` exists only for creating a web-version project under its file-count and attachment limits.

- Local generation must use the complete `{{STYLE_SLUG}}_PROJECT_PACK`, not only this directory.
- This directory contains a reduced, approved, role-balanced export.
- Do not delete a local reference merely because it is absent from the web export.
- Do not treat web limits as local workflow limits.
- Contact sheets, broad collages, backups, duplicates, and rejected files remain local.
- Every exported image must retain its role and provenance in `00_PROJECT_MANIFEST.csv`.
